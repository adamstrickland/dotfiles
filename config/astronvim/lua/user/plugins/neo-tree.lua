return {
  filesystem = {
    -- components = {
    --   icon = function(config, node, state)
    --   end,
    -- },
    filtered_items = {
      hide_dotfiles = false,
    },
    follow_current_file = false,
  },
  window = {
    width = 35,
  }
}
